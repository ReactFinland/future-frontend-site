{
  "tasks": {
    "build": "deno run -A --unstable --no-check https://deno.land/x/gustwind@v0.38.0/gustwind-cli/mod.ts -b -t cpuHalf -o ./build",
    "build:linked": "gustwind -b -t cpuHalf -o ./build",
    "start": "deno run -A --unstable --no-check https://deno.land/x/gustwind@v0.38.0/gustwind-cli/mod.ts -d -p 3000",
    "start:linked": "gustwind -d -p 3000"
  }
}
